<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Set logging level for our test application -->
    <logger name="com.example.camunda" level="DEBUG" />
    
    <!-- Set logging level for Camunda client and process test -->
    <logger name="io.camunda" level="INFO" />
    
    <!-- Reduce noise from other libraries during testing -->
    <logger name="io.grpc" level="WARN" />
    <logger name="io.netty" level="WARN" />
    <logger name="org.testcontainers" level="INFO" />

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>